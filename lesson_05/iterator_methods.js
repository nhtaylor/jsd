// here's our simple array
// try to name each array as plural
const chipmunks = ["alvin", "simon", "theodore"]

// example of anonymous function
// and because each of these is plural, each iterator should be singular
chipmunks.forEach((chipmunk) => {
  // console.log(chipmunk)
  console.log(chipmunk.toUpperCase())
})
